RoomForge / AudioOptimizer — room measurement and audio optimisation kit (Phase 1: exponential sweep generation, FFT/convolution and complex-sum DSP primitives).
Build: `dotnet build AudioOptimizer.sln -c Release`
Test: `dotnet test AudioOptimizer.sln -c Release`
