namespace AudioOptimizer.IO;

/// <summary>Sample encodings this tool writes: IEEE float 32-bit, signed 16-bit and signed 24-bit PCM.</summary>
public enum WavSampleFormat
{
    Float32,
    Pcm16,
    Pcm24,
}
