<UserControl x:Class="AudioOptimizer.UI.Controls.WizardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <!-- §24: twelve rows, presented. The geometry is PINNED rather than measured: a render test probes each row at
         a y it derives from these same numbers, and an auto-sized or wrapping row would move under the probe.
         Row pitch = 26 px (row Height 26, no margin); the first row's top = 8 (root margin) + 22 (guide line) +
         6 (list margin) = 36. Change any of those four numbers and the render test must change with them. -->
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="GuideText" Grid.Row="0" Height="22" FontWeight="SemiBold" Foreground="#FF202020"
                   TextWrapping="Wrap" Text="{Binding GuideText}" />
        <ItemsControl x:Name="StepList" Grid.Row="1" Margin="0,6,0,0" ItemsSource="{Binding Steps}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Height="26">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="170" />
                            <ColumnDefinition Width="62" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <!-- One line per row: a wrapped row would be a taller row, and the probe band is fixed. -->
                        <TextBlock Grid.Column="0" Text="{Binding Heading}" FontWeight="SemiBold" Foreground="#FF202020"
                                   TextTrimming="CharacterEllipsis" />
                        <TextBlock Grid.Column="1" Text="{Binding StateText}" Foreground="#FF404040" />
                        <TextBlock Grid.Column="2" Text="{Binding Detail}" Foreground="#FF404040"
                                   TextTrimming="CharacterEllipsis" />
                        <TextBlock Grid.Column="3" Text="{Binding Cue}" Foreground="#FF606060"
                                   TextTrimming="CharacterEllipsis" />
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
