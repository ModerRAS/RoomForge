namespace AudioOptimizer.UI.Windows;

using System.Windows;

/// <summary>The window is a frame for <c>ShellView</c>; it holds no state and no logic.</summary>
public partial class MainWindow : Window
{
    public MainWindow() => InitializeComponent();
}
